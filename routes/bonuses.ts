import express from "express";

const Router = express.Router();

Router.get("/", () => {});
Router.post("/", () => {});
Router.delete("/", () => {});
Router.get("history", () => {});

export default Router;
